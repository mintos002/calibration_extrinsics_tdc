<?xml version="1.0" encoding="utf-8"?>
<opencv_storage>

  <!-- Path to the output_images folder images will be saved 
          - by default images will be searched in the application folder,
            inside a folder named output_images that follows this scheme:
            -> output_images
                      |-> color
                      |     |-> c_20196141560502490749.png
                      |     |-> ...
                      |-> thermo
                      |     |-> t_20196141560502490749.png
                      |     |-> ...                       
            set the images path surrounded by "". For example: "C:\dir\thermo" -->
  <PathThermoImages>""</PathThermoImages>
  <PathColorImages>""</PathColorImages>
  <!-- Path to the .xml file of calibration parameters (the output of calibration_tcd app) -->
  <InputPath>"out_camera_data_1280.xml"</InputPath>
  <!-- Path to the .xml output file that this app will generate -->
  <OutputPath>"out_camera_data_extrinsics.xml"</OutputPath>
  
  <!-- Delete all files of the output_images file in Path
        - 0 do not delete (default)
        - 1 delete -->
  <DeleteDir>1</DeleteDir>

  <!-- Color and depth image resolution mode and frames per second 
        - IntelResolution_mode 0 = 1280x720 (default)
        - IntelResolution_mode 1 = 640x480
        - IntelResolution_mode 2 = 848x480 
        - Recomended IntelFPS = 30, 15 or 6 ( If app crashes with a defined IntelResolution_mode try other IntelFPS configuration) -->
  <IntelResolution_mode>2</IntelResolution_mode>
  <IntelFPS>30</IntelFPS>
  <!-- Postprocess realsense camera images 
        - 0 Do not postprocess (default) 
        - 1 Postprocess-->
  <IntelPostprocess>0</IntelPostprocess>

  <!-- Flip images
        - 0 no flip (Default)
        - 1 flip thermal 
        - 2 flip color & depth
        - 3 flip all -->
  <FlipMode>0</FlipMode>
  <!-- Do image negative 
        - 0 no negative (default)
        - 1 create negative thermal image
        - 2 create negative color image
        - 3 thermal & color negative images-->
  <NegativeMode>3</NegativeMode>

  <!-- Save modifyed images 
        - 0 to save imagees with selected FlipMode & NegativeMode
        - 1 to save images as default FlipMode & NegativeMode (default) 
        NOTE: To detect the cirle pattern it must appear with white background and black cercles.
        That way if you want to see the circle detection but save the images as default select 0 -->
  <SaveDefaultImages>1</SaveDefaultImages>

  <!-- Detect the asymmetric circle pattern, the pattern must appear with white background and black cercles 
        - 0 Do not detect the pattern (default)
        - 1 Detect the pattern-->
  <AsymCirlePaternDetector>1</AsymCirlePaternDetector>
  <!-- Number of inner corners per a item row and column. (square, circle) -->
  <BoardSize_Width>9</BoardSize_Width>
  <BoardSize_Height>3</BoardSize_Height>

</opencv_storage>